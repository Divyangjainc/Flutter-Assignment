//What will happen if a class implements two interfaces and they both have a method with same name and signature?
//Ans.If a class implements two interfaces, both containing a method with the same name and signature, the class must
//override the method to resolve the ambiguity, otherwise, it will result in a compile-time error
