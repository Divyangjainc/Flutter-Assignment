//Can an interface extend more than one interface in Dart
//Dart supports only single inheritance. It means that a class can extend a single class. It doesn't support multiple
//inheritance, in which a class can extend multiple classes. However, a class can implement multiple interfaces.
